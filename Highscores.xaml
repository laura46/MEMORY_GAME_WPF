<Page x:Class="MemoryProject.Highscores"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MemoryProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Highscores">

    <Grid Background="#75ccdd" Width="auto" Height="auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
            <RowDefinition ></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
            
        <Image Source="Assets/GrootScherm.png" Grid.Column="0" HorizontalAlignment="Center" Height="auto"  Width="auto" Grid.RowSpan="3" Margin="128,-45,128,-49"/>
        <Frame x:Name="navbarFrame" Grid.Row="0" Width="auto"/>
        <Label x:Name="highscores" Grid.Row ="1" HorizontalAlignment="Center" Margin="-50" FontSize="30" Content="Highscores"/>
        <Grid x:Name="scoreGrid" Grid.Row="2"/>
    </Grid>
</Page>
